type PsReportListPropTypes = {
  message: string;
  status: string;
  category: string;
  date: string;
};

const PsReportList = ({
  message,
  status,
  category,
  date,
}: PsReportListPropTypes) => {
  return (
    <div className="w-full bg-white">
      <h1></h1>
    </div>
  );
};

export default PsReportList;
